Claim supported: the dormancy signal measurement brief (2026-09-15) added only
new, additive files - src/taste_engine/dormancy.py, scripts/dormancy_signals.py,
tests/test_dormancy.py, reports/dormancy_signals.md - and modified no tracked
file (`git diff --stat` against the pre-brief tree is empty). Nothing in
selection, scoring, ordering, clustering, or the write path changed, so the
pinned rediscovery eval below must be - and is - byte-identical across two
separate processes.

taste-engine dormancy signal measurement brief, section 5
commit: 5de25bded46253851284019cf71a2f05e11b3b65 (working tree clean at this
        SHA before the brief started; the four new files above are untracked
        additions on top of it, not yet committed at the time this file was
        generated)
run (UTC): 2026-09-15 ~18:0x (two separate `bash scripts/run.sh` invocations,
        each its own Python process)
command: bash scripts/run.sh -m taste_engine.evaluate --split 2026-06-01 --test-end 2026-07-01 -k 50 --half-life 14

Confirmed byte-identical: the command above was run twice, as two separate
Python processes, output redirected straight to a file each time (never piped
through head/tail - see CLAUDE.md Trap 3) and diffed - empty diff.

Also observed, out of this brief's scope: this output's cluster_diverse row
(ndcg@50 = 0.3080) does not match the cluster_diverse row recorded in the
older reports/eval_invariance_window.txt (ndcg@50 = 0.3213, same command,
same day, ~12 hours earlier), which changes the strategy ranking order.
This is not new nondeterminism and not something this brief investigated
further: eval_invariance_window.txt's own commit line records SHA ce76946,
which predates f689446 ("Re-measure embedding-mode comparison; fix
artist_from_channel NaN guard") - a fix already landed on this branch before
this brief started, which changed the text 9 of 2,918 canonical tracks embed
from (CLAUDE.md: "title_artist ARI moved -0.003"). `cluster_diverse` is the
one strategy in this eval whose ranking depends on HDBSCAN cluster
membership, so a small, already-documented embedding change is sufficient
explanation without invoking the hash-seed nondeterminism previously found
in writer._modal_genre. score/most_played/recency (the three strategies
`--mode rediscover` and the README's headline actually depend on) are
unaffected: identical to four decimal places against both this run and
reports/eval_invariance_window.txt's recorded values. Flagged here for the
record; reconciling eval_invariance_window.txt's staleness, if it needs
reconciling, is a separate brief's job, not this one's (brief non-goals:
measurement only, no push).

Full output below is verbatim and unedited.

========================================================================

Temporal hold-out  train < 2026-06-01 <= test (to 2026-07-01)
  half-life               14 days
  train tracks         2,066 (5,465 plays)
  test tracks            867 (1,613 plays)
  seen in train          390 (cold start: 477)
  ceiling@50          100.0%

       strategy  hits  precision@50  ndcg@50  spearman
          score    31          0.62   0.5506    0.3583
    most_played    32          0.64   0.5421    0.4300
        recency    22          0.44   0.3088    0.2620
cluster_diverse    27          0.54   0.3080       NaN

  Best on nDCG@50: score at 0.551 vs 0.542 baseline (+1.6%).
