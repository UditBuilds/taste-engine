# --- Personal data -----------------------------------------------------------
# data/raw/ holds the Google Takeout export: a full year of watch history AND
# search history. This repo is public. Never commit any of it.
data/raw/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/*.sqlite
data/*.sqlite3
data/*.csv
data/*.json

# Derived model artifacts (embeddings). Rebuildable, and large.
data/artifacts/
*.npy

# --- Secrets -----------------------------------------------------------------
.env
.env.*
!.env.example
credentials.json
client_secret*.json
service-account*.json
token.json
token.pickle
*.pem
*.key
*.p12

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/
.cache/
build/
dist/

AUDIT_REPORT.md
